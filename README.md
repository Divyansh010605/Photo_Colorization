# Photo Colorization

This is a Python project for Photo Colorization.

## Setup
1. Install dependencies: pip install -r requirements.txt
2. Run the main script or jupyter notebook to see the results.
